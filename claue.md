# SpecPilot 설계 컨텍스트

> **이 문서의 목적**
> 현재 프로젝트는 **설계 단계**다. 기능 명세, ERD, API 명세서를 작성하는 중이며 구현은 시작하지 않았다.
> 이 문서는 설계 판단에 필요한 배경과 제약, 그리고 이미 내려진 결정과 아직 열려 있는 질문을 정리한 것이다.
>
> **구현 코드를 작성하기 전에 이 문서의 '열려 있는 질문'을 먼저 확인할 것.**
> 확정되지 않은 사항을 확정된 것처럼 가정하고 설계하지 말 것.

---

## 1. 프로젝트 개요

**SpecPilot** — 기획서와 소스코드의 정합성을 자동 검증하고, 요구사항 추적표와 테스트 사양서를 생성하는 시스템.

- **과목**: 한성대학교 온캠퍼스 인턴십 (7분반 8팀)
- **참여기업**: 씽크트리(ThinkTree)
- **팀원**: 김재민, 이시형, 이성규, 정래원
- **기간**: 10주

### 해결하려는 문제

기획서의 요구사항이 실제 코드에 제대로 구현됐는지를 개발자가 수작업으로 대조하고 있다. 이 과정을 자동화한다.

### 설계의 중심 가치

"기획서와 코드를 대조한다"는 발상 자체는 새롭지 않다. 설계가 답해야 할 진짜 질문은 이것이다.

> **자동 판정 결과를 어떻게 신뢰하게 만들 것인가.**

틀린 판정을 검토하는 비용이 수작업보다 크면 아무도 쓰지 않는다.
따라서 모든 설계 판단은 **"판정 근거를 검증 가능하게 만드는가"** 를 기준으로 내린다.

---

## 2. 용어 주의 ⚠️

"SpecPilot"이 두 가지를 가리킨다.

| 표현 | 의미 |
|---|---|
| **SpecPilot** | 우리가 설계·개발하는 검증 도구 (이 프로젝트) |
| **검증 대상 샘플** | 씽크트리가 제공한 로그인/공지사항/마이페이지 샘플 앱. 이것도 이름이 SpecPilot |

샘플 앱의 화면 기획서가 SpecPilot의 **입력 데이터**이고, 샘플 앱 코드가 **대조 대상**이다.
문서나 회의에서 헷갈리지 않도록 후자는 "검증 대상 샘플"로 부른다.

---

## 3. 설계를 제약하는 조건

설계 자유도를 실제로 줄이는 것들. 여기서 많은 구조가 따라 나온다.

### 3.1 운영 시 로컬 LLM 필수

금융·공공 고객사 소스코드를 외부 클라우드로 보낼 수 없다.

**설계에 미치는 영향**

- 모델 크기에 상한이 생긴다 → 복잡한 복합 판단을 LLM에 맡길 수 없다
- 사용자 PC 성능에 좌우된다 → LLM 호출 횟수 자체를 줄이는 설계가 필요
- 로컬 LLM은 vLLM의 Guided Decoding 수준으로 출력 형식을 강제하지 못한다 → **출력 검증 계층이 구조상 필수**
- 개발 중 클라우드 API 사용은 무방 (우리가 쓰는 개발 도구이므로)

### 3.2 입력 문서 포맷이 여러 가지다

발주사 요구사항 분석서(FR-01)는 **Word / PDF / Excel** 을 지원 포맷으로 명시하고 있다.
실제로 받은 샘플 기획서는 PPTX와 PDF 두 형태이며, 앞으로 다른 포맷이 들어올 수 있다.

**설계에 미치는 영향**

- **포맷별 파서를 교체 가능한 구조로 분리**해야 한다. 코드 분석부를 스택별로 나누는 것(3.3)과 같은 이유다
- 파서는 문단 흐름이 아니라 **좌표·구조 기반**이어야 한다. 화면설계서는 어느 포맷이든 도형과 표로 그려지며, 텍스트를 순서대로 뽑으면 화면 목업 라벨과 본문 사양이 뒤섞인다
- 포맷이 달라도 후단이 영향받지 않도록, 파서 출력은 **공통 중간 표현(블록 목록 + 위치 정보)** 으로 통일한다

```
PPTX ─┐
PDF  ─┼─→ [포맷별 파서] ─→ 블록 목록(공통 형식) ─→ 이후 파이프라인
Word ─┤                      block_id, 원문, 물리 위치, 신뢰도
Excel─┘
```

**포맷별 특성**

| 포맷 | 특성 | 주의 |
|---|---|---|
| PPTX | 도형·텍스트박스, 좌표 있음 | `shape.left/top`으로 정렬 가능. 정보 손실이 가장 적다 |
| PDF | 렌더링 결과, 좌표 있음 | 띄어쓰기가 붙고 줄이 갈라진다. 세로쓰기·스캔본 위험 |
| Word | 문단 흐름 + 표 | 텍스트박스·중첩 표·추적 변경 잔존 주의 |
| Excel | 셀 격자 | 병합 셀, 도형 안 텍스트가 누락되기 쉽다 |

**같은 문서라도 포맷에 따라 추출 품질이 다르다.** 실제로 이번 샘플도 PPTX가 PDF보다 깔끔하게 뽑힌다.
따라서 파서는 추출 품질을 스스로 점수화하고(블록별 신뢰도), 낮으면 사람 검수 큐로 보낸다(원칙 3).

- 화면설계서 구조 자체는 규칙적이어서(7.1) 요구사항 분해를 상당 부분 결정론적으로 할 수 있다. 이 장점은 포맷과 무관하게 유효하다

### 3.3 대조 대상이 두 가지 기술 스택이다

Spring Boot 버전과 React 버전이 같은 기획서를 구현했다.

**설계에 미치는 영향**

- 코드 분석부를 **언어/프레임워크별로 교체 가능한 구조**로 분리해야 한다
- 같은 요구사항이 스택마다 다른 계층·다른 형태로 구현된다 (8.1 참조)
- 두 스택 점수 차이가 곧 "일반화 능력"이므로 평가 설계에도 반영

### 3.4 프로젝트 규모가 작다

Spring 샘플이 약 4,000줄(Java 1,400 + 템플릿·JS·설정 2,600).

**설계에 미치는 영향**

- 검색(RAG) 없이 LLM 컨텍스트에 거의 통째로 넣을 수 있다
- **검색 파이프라인을 처음부터 넣을지는 열린 질문**(11장). 대규모 대응은 별도 단계로 미뤄도 된다

---

## 4. 시스템 전체 구조

```
[판정 기준 · 기획서]              [대조 대상 · 소스코드]

기획서 업로드                      코드 입력 (로컬 워크스페이스)
  ↓                                 ↓
블록 분할 + 신뢰도 채점             스냅샷 고정
  ↓                                 ↓
요구사항 추출 (LLM)                 청킹 + 색인
  ↓                                 ↓
사람 검수 → 기준 확정               심볼 · 설정값 테이블
  └──────────────┬──────────────────┘
                 ↓
         요구사항별 근거 검색
                 ↓
      판정 (값 비교는 규칙 / 나머지는 LLM)
                 ↓
         추적표 + 테스트 사양서
```

### 판정 결과 분류

| 상태 | 의미 |
|---|---|
| `implemented` | 구현됨 |
| `partial` | 요구사항 조건 중 일부만 구현 |
| `mismatch` | 구현됐으나 값이나 동작이 기획서와 다름 |
| `not_found` | 관련 코드를 찾지 못함 |
| `needs_review` | 근거는 찾았으나 판정 불가 |
| `not_statically_verifiable` | 정적 분석으로 검증 불가능한 항목 |

**각 상태의 경계 정의는 아직 문서화되지 않았다** (11장 참조). 이것이 설계보다 먼저 정리되어야 할 항목이다.

---

## 5. 확정된 설계 원칙

근거와 함께 기록한다. 근거가 바뀌면 원칙도 재검토 대상이다.

### 원칙 1 — LLM은 좌표를 생성하지 않고 선택한다

**문제**: LLM에게 "몇 번째 줄에 구현됐는지" 물으면 존재하지 않는 줄을 지어낸다.

**설계**: 코드를 미리 청크로 쪼개 `chunk_id`를 부여하고, LLM은 **후보 중 어느 것인지 고르기만** 한다. 파일 경로와 라인 번호는 DB 조인으로 확정한다.

```
❌ LLM 출력: { target_file, start_line, end_line, quoted_code }
✅ LLM 출력: { primary_evidence: chunk_id(후보 enum), status, reasoning }
```

**효과**: 환각을 사후 검증으로 잡는 게 아니라, 표현 자체가 불가능해진다.

**단, 로컬 LLM에서는 enum 강제력이 약하다.** 출력 후 화이트리스트 검증을 두고, 후보에 없는 id가 나오면 `needs_review`로 강등한다.

### 원칙 2 — 분석 시점을 고정한다 (스냅샷)

**문제**: 분석 중 파일이 바뀌면 라인 번호가 밀려 결과가 엉뚱한 곳을 가리킨다.

**설계**: 모든 판정에 `snapshot_id`(커밋 해시 또는 파일 해시 트리)를 함께 저장한다. 결과를 열 때 현재 상태와 비교해 어긋나면 경고한다.

**부가 효과**: 변경분만 재분석하는 증분 처리의 기준점이 된다.

### 원칙 3 — 사람 검수는 기획서 쪽에만 배치한다

**근거**: 기획서는 개정할 때만 바뀌지만 코드는 매일 바뀐다.

**설계**: 요구사항 추출 결과를 한 번 검수해 확정하면 이후 코드 분석은 완전 자동. 검수는 전수가 아니라 **의심 구간만** 큐에 띄운다.

- 파싱 신뢰도가 낮은 블록
- 기획서 원문에 없는 숫자가 추출된 경우
- 어떤 요구사항에도 매핑되지 않은 블록 (누락 의심)

### 원칙 4 — 요구사항 분류는 두 축으로 나눈다

| 축 | 값 | 용도 |
|---|---|---|
| `req_type` | functional / non_functional | 문서·통계 |
| `verifiable` | code / not_statically_verifiable | 파이프라인 분기 |

**근거**: 두 축이 대체로 겹치지만 어긋나는 케이스가 실제로 있다.
"선택된 메뉴는 배경색 Navy"는 기능 요구사항이지만 정적 검증이 어렵고, "비밀번호는 문자가림으로 표시"는 기능이면서 검증 가능하다.

### 원칙 5 (검토 중) — 값 비교는 규칙 엔진으로

"제목 50자", "첨부 50MB"는 숫자 비교 문제지 추론 문제가 아니다. 기획서에서 값을 뽑고 코드에서 실효값(상수 → 설정 파일 → 프로파일 override)을 추적해 코드로 비교한다.

**단, 이건 검증되지 않은 가정이다.** 최신 LLM은 단순 비교를 잘한다. 규칙 엔진 구현 비용도 스택마다 따로 들어간다.
→ **먼저 LLM으로 처리해보고 실제 오류가 관측되면 그때 도입**한다.

---

## 6. 데이터 모델 설계 방향

ERD 작성 시 참고. 각 항목에 설계 근거를 적어둔다.

```
snapshot          repo_id, commit_sha, indexed_at
                  → 모든 분석은 특정 시점에 고정된다 (원칙 2)

chunk             id(PK), snapshot_id, chunk_type, file_path,
                  start_line, end_line, symbol_fqn, content
                  → 파일·라인 좌표의 유일한 출처 (원칙 1)

requirement       stable_key, doc_version, seq_label, body,
                  req_type, verifiable, parse_confidence
                  → stable_key는 개정 간 승계되는 논리 ID
                  → seq_label(문서상 번호)은 개정 시 밀리므로 표시용으로만

verdict           requirement_id, snapshot_id, status, confidence,
                  decided_by, input_hash
                  → input_hash는 동일 입력에 동일 결과를 보장하는 캐시 키

verdict_evidence  verdict_id ↔ chunk_id (N:M), role, rank
                  → 요구사항 1건에 근거가 여러 개 붙는다
```

### 설계 시 주의할 점

- `verdict_evidence`는 `chunk_id`만 참조하고 **파일 경로·라인 번호를 저장하지 않는다.** 조인으로 가져온다
- 근거는 **항상 배열**. 요구사항 하나가 컨트롤러·서비스·설정 파일에 흩어지는 것이 정상이다. 단수형 필드로 설계하면 나중에 프론트까지 전면 수정해야 한다
- 같은 요구사항(비밀번호 규칙)이 여러 화면에 반복 등장한다. 요구사항은 화면별로 별개로 두되 근거 청크는 공유할 수 있어야 한다 → N:M이 필요한 실제 이유

---

## 7. 입력 데이터 구조 (실물 기준)

씽크트리가 2026-09-18 전달한 자료를 분석한 결과.

| 자료 | 역할 |
|---|---|
| 화면 기획서 ver0.2 (PPTX/PDF) | **SpecPilot의 입력** |
| 테스트 케이스 및 결과 (XLSX) | **출력 양식 견본** (정답지가 아니라 형식 참고용) |
| Spring 샘플 코드 | 대조 대상 ① — Java 21, Spring Boot 3.5.16, JPA, Thymeleaf, H2 |
| React 샘플 코드 | 대조 대상 ② — React+Vite, Express, SQLite |

> 씽크트리가 **일부 기능을 의도적으로 구현하지 않았다.** 그것이 SpecPilot이 찾아내야 할 정답이다.

### 7.1 기획서 구조가 규칙적이다

모든 슬라이드가 같은 골격이다.

```
화면ID (LOGIN-001) + 화면명 + Depth
Description:   0, 1, 2, 3 ...   (화면 요소별 사양)
Action/Event:  A, B, C, D ...   (버튼 동작)
```

**설계상 의미**

- 번호 체계가 이미 요구사항 단위를 정의하고 있다 → LLM이 "무엇이 요구사항인지" 판단할 필요가 없다
- `stable_key`를 `LOGIN-001-2`, `NOTICE-002-A` 형태로 자동 생성할 수 있다
- LLM 역할이 "요구사항 추출"에서 **"제약 조건 정규화"** 로 축소된다 → 정확도 부담이 크게 줄어든다

### 7.2 파싱 시 고려할 점

- PPTX는 도형/텍스트박스 기반이라 **삽입 순서대로 뽑으면 화면 목업 라벨과 본문 사양이 뒤섞인다.** 도형 좌표(`shape.left`, `shape.top`)로 그룹핑·정렬하는 전처리가 필요
- PDF보다 PPTX가 낫다 (PDF는 띄어쓰기가 붙고 줄이 갈라짐)
- 원문에 오타와 오류가 섞여 있다 (`게이 종료일`, `청부파일`, 잘린 문장, 앞뒤 모순)
  → **"기획서가 틀렸고 코드가 맞는 경우"를 어떻게 분류할지** 결정이 필요하다

### 7.3 출력 양식

테스트 사양서 견본 구조. 시트 6개(로그인/회원가입/비밀번호찾기/공지사항/마이페이지/권한), 시험번호 37개, 세부 단계 59개.

```
시험번호 / 시험구분 / No / 시험절차 / 예상결과 / 계정 / 입력값 / 비고 / 확인결과(O·X)
```

**설계상 의미**: 출력에 필요한 정보가 판정 단계에서 수집되어야 한다.
예를 들어 "계정", "입력값" 컬럼을 채우려면 판정할 때 진입 조건과 테스트 데이터를 같이 모아둬야 한다. 나중에 추가하려면 파이프라인을 다시 돌려야 한다.

---

## 8. 대조 대상 분석 결과

정적 분석 기준. 실행 검증은 아직 하지 않았다.

### 8.1 같은 요구사항이 스택마다 다른 자리에 있다

| 요구사항 | Spring | React |
|---|---|---|
| 제목 50자 / 내용 2000자 | 서버 `@Size` + 화면 `maxlength` | **화면 `maxLength`만** (서버 검증 없음) |
| 첨부 50MB | `application.yml` + 서비스 코드 | 서비스 코드 상수만 |
| 전화번호 형식 검증 | 없음 | 있음 |
| 삭제 확인 팝업 | 없음 | 있음 |
| 비밀번호 8자 + 조합 | `PasswordValidator` 한 곳 | 두 서비스에 중복 |

**설계상 의미**

- 코드 분석부는 스택별로 교체 가능해야 한다 (3.3)
- 설정 파일도 분석 대상에 포함되어야 한다 (Spring의 50MB는 `yml`에 있다)
- React는 `client/`와 `server/`를 모두 봐야 한다. 한쪽만 보면 오판한다

### 8.2 의도적으로 빠진 것으로 보이는 기능

| 요구사항 | 상태 |
|---|---|
| 소셜 로그인 (Google/Naver) | **버튼만 있고 동작 없음** |
| 게시기간·게시여부 OFF 글 숨김 | 필터 없이 전부 노출 |
| 조회수 유저당 1회 | 볼 때마다 +1 |
| 확인 팝업 (로그아웃, 등록취소, 삭제) | Spring 전무 / React는 삭제만 |
| 목록 비었을 때 안내 문구 | 없음 |

**소셜 로그인이 설계 관점에서 중요하다.** 화면에 "Google로 계속하기" 버튼이 있어 검색에 걸린다. 근거를 찾았다고 바로 `implemented`로 판정하면 틀린다.
→ **근거 존재와 기능 구현이 다르다**는 것을 판정 로직이 구분할 수 있어야 한다.

### 8.3 불일치 사례

| 기획서 | 코드 |
|---|---|
| 날짜 YYYY/MM/DD | `yyyy.MM.dd` |
| 비번찾기 에러 2종 구분 | 구분하지 못함 |
| 수정 후 목록 이동 | 상세로 이동 (옛 기획 버전을 따름) |
| 삭제는 수정화면 + 팝업 | 상세화면에서 즉시 삭제 |
| 에러 메시지 문구 | 전부 다름 |

---

## 9. 평가 설계 (요구사항 분석서에 누락된 부분)

발주사 요구사항 분석서에 정확도 평가 관련 내용이 전혀 없다. 우리가 설계해야 한다.

### 골든셋은 우리가 만든다

| | 출처 |
|---|---|
| 기획서 | 씽크트리 제공 |
| 소스코드 | 씽크트리 제공 |
| **정답 라벨** | **팀이 직접 작성** |

샘플 앱을 실행해 기획서 항목대로 확인하고 결과를 표로 정리하면 그것이 골든셋이다.

### 측정 지표는 두 벌로 분리한다

| 구분 | 지표 |
|---|---|
| 추출 | 요구사항 추출 precision / recall |
| 검색 | Recall@K (정확도의 상한선) |
| 판정 | 분류별 P/R, 특히 mismatch recall |
| 근거 | 예측 청크와 정답 청크의 일치도 |
| 안정성 | 오탐율 (implemented → mismatch) |
| 최종 | 수작업 대조 시간 절감률 |

**추출과 판정을 분리 측정해야 병목이 어디인지 보인다.** 합쳐서 재면 원인 분석이 불가능하다.

### 한계를 기록한다

현재 확보 가능한 골든셋은 UI 폼 검증 중심의 한국어 기획서 1종이다. 배치·권한 등 다른 요구사항 유형과 일본어 문서에 대한 일반화는 검증되지 않는다. 이 한계를 문서에 명시한다.

---

## 10. API 설계 방향

명세서 작성 시 참고.

### 전송 채널이 나뉜다

분석이 수 분 걸리므로 동기 응답으로 묶을 수 없다.

| 채널 | 용도 |
|---|---|
| 분석 시작 | 202 즉시 반환 + 작업 ID |
| 진행·결과 | SSE 스트리밍 (판정 1건씩) |
| 재조회·필터 | REST JSON |
| 산출물 | 바이너리 다운로드 (xlsx) |

### 응답 설계 규칙

- `evidence`는 **항상 배열**
- `status`는 **enum 코드로만** 전송. 화면 표시용 한국어 라벨은 프론트에서 처리
- `snapshot_id`를 항상 포함 (원칙 2)
- 판정 근거의 종류가 다르므로 설명 필드를 판별 유니온으로 설계
  (값 비교 / LLM 판단 / 검색 실패는 화면에 다르게 표시되어야 한다)

---

## 11. 아직 열려 있는 질문

**설계 중 이 항목들을 확정된 것으로 가정하지 말 것.**

| 항목 | 상태 | 결정에 필요한 것 |
|---|---|---|
| 5분류 상태의 경계 정의 | 미정 | 라벨 정의서 작성. **가장 시급** |
| 에러 문구 차이를 불일치로 볼지 | 미정 | 라벨 정의서 |
| 화면에만 검증이 있는 경우의 판정 | 미정 | 라벨 정의서 |
| 기획서가 틀린 경우의 분류 | 미정 | 별도 상태 신설 여부 검토 |
| 검색(RAG) 도입 여부·시점 | 미정 | 샘플 규모로는 불필요할 수 있음 |
| 값 비교 규칙 엔진 필요 여부 | 미정 | LLM으로 먼저 돌려보고 오류 관측 |
| AI 모델 선정 | 미정 | 샘플 기준 비교 |
| 리랭커 채택 여부 | 미정 | 도입 전후 Recall@K 비교 |
| 사용자 PC 사양 가정 | 미정 | 로컬 LLM 크기를 좌우. 멘토·발주사 확인 필요 |

---

## 12. 설계 시 지켜야 할 것

- LLM에게 파일 경로나 라인 번호를 **생성**시키는 구조를 만들지 말 것. 항상 후보 중 선택
- LLM 호출은 **한 모듈에 격리**. 클라우드/로컬 교체가 설정 변경으로 가능해야 함
- 근거 필드는 항상 배열. 단수형으로 설계하면 나중에 전면 수정
- 코드 분석부는 스택별 교체 가능한 구조로 분리
- 설정 파일(yml, properties)도 분석 대상에 포함
- 정적 분석으로 검증 불가능한 요구사항은 판정을 시도하지 말고 별도 분류
- **추측으로 최적화하지 말 것.** 병목이 어디인지는 측정 후에 안다